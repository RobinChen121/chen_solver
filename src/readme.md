This folder contains the solver implementation files.

- `core/`: core model data structures and orchestration logic
- `io/`: model readers/writers (MPS/LP, etc.)
- `presolve/`: presolve and reductions